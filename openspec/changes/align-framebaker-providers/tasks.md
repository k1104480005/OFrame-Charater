## 1. Provider 数据模型与迁移

- [x] 1.1 扩展 Provider 配置模型，支持协议类型、显示名、图片/视频/文本模型列表、尺寸和能力元数据，并通过现有配置单元测试验证默认值与旧字段兼容
- [x] 1.2 实现设置读取归一化与迁移，兼容现有 Doubao/OpenAI/Agnes/compatible 配置、旧单模型字段和环境变量密钥，并通过重启恢复测试验证不丢配置
- [x] 1.3 实现快捷预设的后端默认描述（FrameBaker 七项：OpenAI、百炼、banana/Gemini、MiniMax、火山方舟/豆包、自定义 CLI、自定义 API，外加人工验收要求的 Agnes 专项多模态预设），并通过预设快照测试验证类型、默认地址和模型分类正确
- [x] 1.4 扩展 Provider 能力声明与校验，确保图片/视频/文本能力不匹配时在外部调用前返回明确错误，并通过边界测试验证零外呼

## 2. 协议适配器

- [x] 2.1 保持并整理现有 OpenAI 兼容适配器，统一认证、超时、响应大小限制和错误解析，并通过 fake transport 验证图片、文本和引用图请求
- [x] 2.2 实现百炼适配器，覆盖兼容模式与原生图片/视频请求所需的端点、认证、尺寸和响应解析，并通过 fake transport 验证成功与错误响应
- [x] 2.3 实现 banana/Gemini 图片适配器，覆盖 generateContent、API key、inlineData 引用图和响应图片解析，并通过 fake transport 验证请求体与错误处理
- [x] 2.4 实现 MiniMax 图片适配器，并保留视频模型配置接口；通过 fake transport 验证图片请求、单引用图限制和非标准错误响应
- [x] 2.5 实现火山方舟/豆包专用适配器，覆盖 Seedream 图片请求、引用图字段、尺寸和响应解析；通过 fake transport 验证不再错误套用其他协议
- [x] 2.6 扩展模型列表和连接测试适配逻辑，按协议选择正确的模型端点和认证方式；通过 fake transport 验证各协议的请求路径
- [x] 2.7 将协议适配器接入 Provider registry 与生成服务，保证显式 Provider/模型路由且不静默回退；通过路由测试验证每种类型使用正确适配器

## 3. 自定义 CLI Provider

- [x] 3.1 增加自定义 CLI 的结构化配置字段和七预设中的 CLI 草稿，并通过配置序列化测试验证字段持久化
- [x] 3.2 实现 argv 构建、上下文取消和输出文件校验，禁止 shell 字符串插值；通过参数边界测试验证空格和特殊字符不会改变参数
- [x] 3.3 实现引用图支持预检、退出码/输出缺失/格式错误报告，并通过 fake executable 或命令边界测试验证失败状态
- [x] 3.4 保留旧模板配置读取兼容，设置页默认不再暴露模板编辑；通过旧配置迁移测试验证历史 Provider 仍可运行

## 4. Wails API 与模型选择

- [x] 4.1 扩展 ProviderInfo/ProviderConfig Wails 模型，暴露协议类型、能力分类、视频模型和预设元数据，并重新生成 TypeScript bindings 后通过 typecheck
- [x] 4.2 增加基于草稿配置的连接测试 API，确保不保存配置、不切换 active Provider，并通过 fake transport 与持久化快照测试验证
- [x] 4.3 增加基于草稿配置的模型获取 API，返回可过滤的模型列表且不保存配置，并通过 fake transport 验证
- [x] 4.4 实现按图片/视频/文本能力筛选 Provider 和模型的共享服务逻辑，并通过选择矩阵测试验证不兼容项被拒绝
- [x] 4.5 将选定 Provider、模型和能力写入生成确认快照，并通过确认前零外呼测试验证 Provider/模型不会在执行中漂移

## 5. 设置界面

- [x] 5.1 将设置面板快捷添加扩展为 FrameBaker 七项与 Agnes 专项，并为每项展示协议标签、字段说明和配置状态；通过前端构建和手动点击验证
- [x] 5.2 将 Provider 卡片字段按协议切换，支持图片/视频/文本模型独立输入、尺寸和 CLI 参数；通过前端 typecheck 与表单手动验证
- [x] 5.3 让“测试连接”和“获取模型”使用当前未保存草稿值，并以内联状态展示延迟、模型列表和错误；通过 mock binding 或手动输入验证不触发保存
- [x] 5.4 增加模型列表过滤、能力分类目标和 chips 点选，保留用户手输模型；通过前端交互测试验证加入/移除行为
- [x] 5.5 增加 Provider 与文本模型/提示词增强模型关联管理，并通过保存、重载和缺失 Provider 场景验证
- [x] 5.6 保持密钥遮罩、环境变量回退、本地统计和主题；人工验收更新：不固定预置内置 Provider，所有 Provider（含内置身份）可删除并可重新添加（隐私检查与手动设置流程见人工清单）

## 6. 生成与未来视频抽帧准备

- [x] 6.1 将图片生成入口接入新的 Provider/模型选择器，并通过无可用 Provider、未配置模型和显式选择场景验证
- [x] 6.2 增加视频抽帧配置读取接口和能力检查，但在视频执行管线尚未完成前明确阻止实际视频调用；通过配置恢复和 unsupported 错误测试验证
- [x] 6.3 更新生成确认界面，展示协议、Provider、模型、能力、调用量和重试上限；通过确认取消零外呼测试验证
- [x] 6.4 验证现有镜像方向、质量验收、任务恢复、缓存和重试行为在 Provider 扩展后不回归；通过完整 Go 测试套件验证

## 7. 文档、自动化验证与发布

- [x] 7.1 更新用户指南、CLI 指南和 Provider 配置文档，记录七个预设、模型能力分类、密钥隐私和视频模型预配置方式，并通过文档路径检查
- [x] 7.2 增加所有协议、迁移、连接测试、模型获取、能力筛选、CLI 安全执行和重启恢复的 fake transport/单元测试，并通过 `go test -count=1 ./...`
- [x] 7.3 完成前端 `npm run typecheck`、`npm run build`、像素图集检查和 Wails bindings 生成验证
- [x] 7.4 使用真实前端嵌入流程构建便携版，检查 EXE 不含 `Frontend not built yet` 占位文案并启动验证
- [x] 7.5 使用 NSIS 3.12 PATH 配置构建安装包，验证安装包和便携版产物存在、可启动，并记录 SHA-256
- [ ] 7.6 执行七预设人工验收、无密钥边界、未保存草稿测试、Provider 切换、模型分类、视频模型保存恢复和 CLI 失败恢复，并更新人工测试清单
