# 角色动画资产工作台

面向独立游戏与像素游戏开发者的 2D 角色动画资产制作上下文：以「角色身份包」为根对象，将文字、参考图或既有精灵转化为可直接进入游戏引擎的动画资产。首发形态为本地优先桌面工作台，全部流程围绕统一、可版本化的根对象组织。

## Language

### 根对象与产物

**角色**:
一个可被识别、可复用的游戏内角色实体，是资产创作的对象。
_Avoid_: 单位、玩家、小人

**角色身份包**:
整个工作流的根对象；一个可版本化的容器，承载某角色从身份定义到动画资产的全部内容与元数据。
_Avoid_: 角色文件、工程、项目

**动作**:
角色在游戏中可触发的一段独立动画行为，如待机、走路、攻击。
_Avoid_: 动画、技能

**方向集**:
同一动作在多个朝向下的帧序列组织方式；动作由方向集构成，每个方向对应一份独立帧序列。

**动画资产**:
可直接导入游戏引擎的成品交付物，由动作、方向集的帧序列及锚点信息构成。
_Avoid_: 素材、精灵

**帧**:
动画资产中一张独立的静态像素画面，是构成动画序列的最小图像单元。
_Avoid_: 图片、贴图

**锚点**:
附着于帧或角色上的定位参考点，用于对齐与挂载，如脚底、手持点。
_Avoid_: 原点、坐标

**导出包**:
面向特定游戏引擎的目标格式打包产物，可从角色身份包生成。

### 生成与质量

**生成 provider**:
提供文字或图像生成能力的外部服务，是生成流程的能力来源。
_Avoid_: 模型、接口

**生成候选**:
一次生成流程产出的、尚未通过质量验收的中间结果。

**确定性校正**:
对生成结果施加的人工或规则化修正，使输出符合帧、锚点等确定性约束。

**质量验收**:
判定生成结果是否达到可交付标准的关口；通过验收的生成候选才可成为动画资产。
_Avoid_: 检查、审核

**质量评分**:
对生成候选或成品进行量化评价的指标值，用于辅助质量验收。

### 工作流与空间

**工作区**:
用户在本机组织与管理角色身份包的本地空间。
_Avoid_: 目录、文件夹

**素材**:
进入流程的原始输入材料，如参考图、已有精灵。
_Avoid_: 资产、资源

**任务**:
工作流中一次可执行、可追踪的工作单元。

**流程**:
从入口（文字、参考图或精灵）到导出包的完整步骤序列。

**持久化会话**:
跨多次打开与关闭保持连续状态的用户会话，承载流程中断后的继续能力。

### 版本与决策

**身份版本**:
角色身份包在一次明确的外观修订后形成的不可变版本；基于旧版本的动画资产仍可保留，但不再默认代表当前身份。
_Avoid_: 草稿、快照

**候选接受**:
用户将一个生成候选明确选定为当前动画资产的业务动作；未被接受的候选只属于候选历史。
_Avoid_: 自动通过、发布

**逻辑画布**:
角色身份包约定的统一动画单元尺寸与坐标范围，所有动作和方向共享该规格。
_Avoid_: 图片尺寸、窗口大小

**生成确认**:
用户在可能产生外部 provider 调用前确认预计调用量和最大重试次数的业务决策。
_Avoid_: 提交、开始
