# 角色动画资产工作台 — 项目计划

> 面向独立游戏与像素游戏开发者的 2D 角色动画资产制作工具（工作代号：OFrame Character）。
> 本文件为简洁完整的总计划：边界、架构、关键机制、界面与视觉、分阶段实施、风险。只规划，不实现。

---

## 1. 产品边界

### 1.1 做什么

- **根对象**：以「角色身份包」为核心的可版本化容器，承载某角色从身份定义到动画资产的全部内容与元数据。
- **流程覆盖**：从入口（文字描述、参考图、既有精灵）经生成、确定性校正、质量验收，到可直接导入游戏引擎的动画资产与导出包。
- **目标用户**：独立游戏 / 像素游戏开发者；资产面向 2D 像素角色动画（动作 × 方向集 × 帧序列 × 锚点）。
- **形态**：首发为**本地优先桌面工作台**，离线可用核心编辑能力，联网时才调用生成 provider。

### 1.2 明确不做（非目标）

- 不做 3D、骨骼/网格动画、物理仿真；仅做 2D 逐帧像素动画。
- 不做通用图像编辑器；仅提供满足「确定性校正」的轻量编辑。
- 不做云端协作/多人实时编辑；身份包为单机文件（可复制、可归档）。
- 不做生成模型训练；生成能力全部来自外部 provider。
- 不做游戏引擎运行时；只产出导入引擎的资产与导出包。

### 1.3 成功判据（产品层）

- 一个角色从文字/参考图到 4 方向行走动画资产，全流程可在一个工作会话内完成且可中断恢复。
- 方向一致性达标（同一动作不同方向同帧节奏、同体型、同锚点），无需逐帧手工重绘。
- 产出资产可被 Godot / Unity / 自研引擎直接消费（导出包）。

---

## 2. 架构：Go/Wails + React/PixiJS + oframe CLI

### 2.1 分层总览

| 层 | 技术 | 职责 |
|---|---|---|
| 桌面壳 | **Go + Wails** | 窗口、本地文件系统、身份包存取、任务队列、provider 调用、版本管理、导出 |
| 前端 UI | **React + TypeScript** | 三标签界面、表单与工作流交互、状态管理 |
| 预览/画布 | **PixiJS** | 帧序列回放、锚点可视化、PixelPerfect 像素预览、轻量编辑画布 |
| 命令行 | **oframe CLI**（Go 同源） | 与 GUI 共享核心库的可脚本化管线：批量生成、校验、导出，供 CI 与高级用户使用 |

### 2.2 核心原则

- **单核多端**：领域逻辑（身份包模型、方向集、版本、导出）全部放在 Go 核心库；Wails GUI 与 oframe CLI 只是同一核心的两个入口，保证行为一致。
- **前端无状态**：React 层只做展示与交互，所有持久化与业务规则由 Go 侧执行；Wails 绑定作为唯一通信通道。
- **provider 可插拔**：统一 Provider 接口（文本/图像生成），按厂商实现适配器，运行时可切换、可配置密钥与额度。

### 2.3 关键模块（Go 核心库）

- `identity`：角色身份包模型、逻辑画布规格、锚点定义。
- `motion`：动作、方向集、帧序列的数据结构与操作。
- `pipeline`：PerfectPixel filmstrip 确定性生成管线（见 §4）。
- `provider`：Doubao / gpt-image-2 / Agnes 等适配器与限流、重试。
- `task`：可恢复任务队列（见 §6）。
- `edit`：轻量编辑指令（见 §7）。
- `version`：身份版本与修订历史（见 §8）。
- `export`：导出包生成（引擎目标格式）。

---

## 3. 平台与外部服务策略

### 3.1 Windows 首发

- 首发目标：**Windows 10/11 x64**，单机本地优先；安装包（NSIS/免安装便携版二选一提供）。
- 原因：目标用户（独立/像素游戏开发者）主平台为 Windows；Wails 在 Windows 上的打包与系统集成最成熟。
- 预留：核心库与前端保持平台无关，macOS/Linux 列为 P3 之后的扩展项，不做首发承诺。

### 3.2 生成 Provider 策略

| Provider | 定位 | 说明 |
|---|---|---|
| **Doubao（豆包）** | 默认主力 | 文本/图像生成，性价比与国内可用性优先，作为默认生成 provider |
| **gpt-image-2** | 高质量备选 | 需要更高画质/一致性时切换，成本较高 |
| **Agnes** | 专项备选 | 按实际接入情况作为补充/对比 provider |

- **生成确认**：任何可能产生外部调用的操作，先展示预计调用量与最大重试次数，经用户确认后执行（对应领域概念「生成确认」）。
- 密钥、额度、调用统计在本地管理；失败自动重试并有上限，杜绝静默超支。

---

## 4. 方向策略与 PerfectPixel 确定性管线

### 4.1 方向策略：默认单方向 + 自动镜像

- **默认单方向**：新动作默认只生成一个方向（通常为右向/正面），成本最低、迭代最快。
- **4/8 方向自动镜像**：用户选择 4 或 8 方向时，仅对**基本方向**发起生成调用，其余方向通过**水平翻转镜像**派生：
  - 4 方向：右（生成）→ 左（镜像）；上/下（生成）。
  - 8 方向：右（生成）→ 左（镜像）；上、下、右上→左上镜像、右下→左下镜像（共 4 次生成 + 4 次镜像）。
- 镜像方向自动生成独立帧序列，锚点按镜像规则换算；手型/不对称细节默认保留镜像结果，允许用户在验收时手动替换某方向（消耗额外调用并计入生成确认）。

### 4.2 PerfectPixel filmstrip 确定性管线

核心思想：**一次生成整条「胶片条」（filmstrip），保证帧间一致性；再确定性切片与校正**。

1. **规格化**：依据逻辑画布（统一单元尺寸与坐标范围）将动作的帧序列规格化为定长、定序的帧清单。
2. **filmstrip 生成**：将同一动作的所有帧按帧序横向拼成一张长条图（filmstrip），一次提示词生成 → 帧间风格、体型、节奏天然一致。
3. **确定性切片**：按规格化坐标对 filmstrip 精确切片为独立帧；几何变换（缩放/位移）使用**整数像素级**规则，不引入二次插值模糊。
4. **确定性校正**：自动校正锚点（脚底、手持点等）到规格坐标；透明背景处理；裁边与留白按逻辑画布对齐。
5. **PixelPerfect 预览**：以最近邻采样（pixel-perfect）在 PixiJS 画布回放切片结果，所见即引擎所得；支持网格叠加与锚点可视化校对。
6. **验收**：管线输出进入质量评分与候选接受流程，通过后方可成为动画资产。

---

## 5. 质量评分与验收

- **质量评分**：对每个生成候选输出量化指标，辅助「质量验收」关口：
  - 结构指标：切片完整性（帧数、尺寸、透明通道占比）、锚点偏差、帧间面积/重心抖动。
  - 规则指标：色数/调色板一致性、镜像方向对称性、越界像素占比。
  - 可选 AI 辅助：provider 或本地模型对「同角色一致性」的粗评分（仅作参考，不阻塞流程）。
- **质量验收**：评分达到阈值 + 用户在 PixelPerfect 预览中确认，才算通过；未通过进入重试（新 filmstrip 生成）或人工轻量编辑。
- 评分与验收结果全部写入身份包元数据，作为候选历史的一部分，供版本化修订追溯。

---

## 6. 可恢复任务

- **任务模型**：工作流中的每次生成/校正/导出均为可追踪任务，持久化到本地队列（含 provider 参数、调用量、状态、进度、错误与重试计数）。
- **持久化会话**：任务队列与用户会话跨应用开关保持连续状态；应用中断（崩溃、关机、网络失败）后重启可一键续跑未完成任务。
- **幂等与去重**：同一任务的已成功结果缓存，避免重复调用计费；重试遵循「生成确认」约定的最大次数上限。
- **可见性**：三标签界面中提供任务进度面板（进行中/排队/失败），失败可查看原因并重试或放弃。

---

## 7. 轻量编辑

- 定位：**确定性校正的最小闭环**，而非完整图像编辑器。
- 能力清单：
  - 帧级：裁切、透明背景清理（去边/去残留）、像素级替换（橡皮/取色/单像素笔刷）。
  - 序列级：帧顺序调整、插入/删除帧、帧时长（节奏）调整。
  - 锚点级：拖拽调整锚点，应用于单帧或整方向集。
  - 批量：将同一校正（如统一去背景）批量应用到方向集内全部帧。
- 约束：所有编辑以**可回放的编辑指令**记录（见 §8 版本化），非破坏式存储；编辑结果可重新进入质量评分。

---

## 8. 版本化修订

- **身份版本**：角色身份包在明确外观修订后形成**不可变身份版本**；旧版本动画资产仍保留，但不再默认代表当前身份。
- **候选接受**：用户将生成候选明确选定为当前动画资产的业务动作；未接受候选只属于候选历史，不进入资产。
- **修订历史**：所有变更（生成、编辑、接受、镜像替换）以追加式操作日志记录，支持回退到任一历史点；历史与版本均存于身份包内。
- **与文件系统关系**：身份包为目录+清单（manifest）结构，便于版本管理与外部备份；导出包从指定身份版本生成。

---

## 9. 界面结构：启动页与三标签

### 9.1 启动页：身份包入口

- 启动页**只做**选择或创建角色身份包：选择已有身份包进入工作台，或创建新的身份包；不承载任何编辑能力。
- 身份包为本地目录 + manifest 结构（见 §8），可复制、可归档。

### 9.2 三标签主界面

进入后顶层固定为三个标签，对应「制作 → 验收 → 导出」主流程：

| 标签 | 名称 | 内容 |
|---|---|---|
| **① 制作** | 动作与制作 | 内部切换「身份 / 动作 / 编辑」三个子页（见 9.3） |
| **② 验收** | 质量验收 | 质量评分、PixelPerfect 预览回放与确认、候选接受/拒绝、方向替换 |
| **③ 导出** | 资产与导出包 | 动画资产检视（帧序列 + 锚点清单）、引擎目标选择、导出包生成与校验、导出历史 |

- 标签间数据共享同一身份包实例；未完成动作在标签间切换不丢失。

### 9.3 制作内部子页

- **身份**：身份定义（文字/参考图/既有精灵入口）、逻辑画布规格、锚点预设、身份版本历史。
- **动作**：动作列表、方向集配置（单方向 / 4 / 8 自动镜像）、filmstrip 生成（进度见全局任务抽屉）。
- **编辑**：轻量编辑（帧级裁切/透明清理/像素替换、序列级帧顺序/时长、锚点调整、批量应用）。

### 9.4 全局入口

- **设置**：齿轮入口，承载全局配置（provider 选择、密钥/额度、外观主题等）。
- **任务**：全局抽屉，跨标签可见，展示进行中/排队/失败任务；失败可查看原因并重试或放弃。

---

## 10. 界面视觉：暖白/深墨双主题

- **主题**：暖白 / 深墨双主题（明暗两套，可切换），像素化工作台质感；整体克制，不牺牲信息密度与可读性。
- **边框与分隔线**：控件与面板统一使用像素边框与像素分隔线（点阵描边），不用圆角与阴影堆砌层次。
- **间距**：统一 8px 间距栅格，布局按 8px 的倍数对齐。
- **状态色**：青绿（成功/进行中）与橙红（警告/失败）仅用于状态表达，不做装饰性使用。
- **像素字体**：少量使用像素字体（标题、状态、数值等点缀场景，如 Press Start 2P 或自绘点阵字）；正文保持高可读的常规字体。
- **洋红专用**：洋红色仅用于抠图（透明背景处理）的技术性背景（如去背预览、alpha 检查视图），不得出现在常规界面。
- 视觉系统以 CSS/主题令牌实现，与功能解耦，允许后续换肤。

---

## 11. 分阶段实施计划

### 阶段 P0 — 骨架与单方向 MVP（约 4–6 周）
- Go 核心库：身份包模型、逻辑画布、单方向帧序列、锚点。
- Wails 壳 + React 三标签外壳 + PixiJS 预览画布；复古掌机视觉基础。
- Doubao 适配器 + 生成确认 + 简单任务队列（可恢复的 MVP 形态）。
- filmstrip 管线 v1：单方向、切片、锚点校正、PixelPerfect 预览。
- 验收标准：文字描述 → 单方向待机/行走 → 预览回放 → 手动接受 → 基础导出。

### 阶段 P1 — 方向集与质量闭环（约 4–6 周）
- 4/8 方向自动镜像全流程；镜像锚点换算与方向替换。
- 质量评分体系 + 候选历史 + 质量验收关口。
- 轻量编辑 v1（裁切、去背景、锚点调整、帧顺序/时长）。
- 可恢复任务完善：崩溃续跑、失败重试、调用量统计。
- 版本化修订 v1：身份版本、候选接受、操作日志回退。
- 验收标准：4 方向行走资产全流程可断点续跑完成；gpt-image-2 / Agnes 可切换。

### 阶段 P2 — 打磨与发布（约 3–4 周）
- 导出包多引擎（Godot/Unity/通用序列帧）与校验。
- oframe CLI 对齐 GUI 核心能力并输出文档。
- Windows 安装包、自动更新、性能与内存优化、示例资产与教程。
- 验收标准：公开 Beta 发布，外部用户可完成一个完整角色资产。

### 阶段 P3 — 扩展（不在首发承诺内）
- macOS/Linux、批量任务与批处理、更多 provider、模板与风格库、社区导出格式。

---

## 12. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| **provider 生成不一致**（同角色不同帧/方向走样） | 方向与帧一致性差，返工成本高 | 默认 filmstrip 一次生成保证帧内一致；自动镜像减少生成次数；AI 一致性粗评分辅助；保留候选历史可重试 |
| **镜像方向动画不自然**（持械、非对称服装） | 镜像结果被拒，需额外调用 | 允许按方向手动替换并计入生成确认；文档明确镜像语义 |
| **生成成本失控** | 用户不满、项目口碑受损 | 生成确认前置、调用量预估、去重缓存、失败重试上限、本地调用统计 |
| **Wails 打包/系统集成问题**（Windows 特有坑） | 发布延期 | P0 早期做最小打包验证；NSIS 与便携版并行验证 |
| **PixiJS 性能**（多帧大画布回放卡顿） | 预览体验差 | 纹理图集化、最近邻渲染、按需加载帧、降级缩放模式 |
| **质量评分主观性强** | 评分不可信、流程受阻 | 评分只做辅助，验收以人审为准；指标侧重可量化结构项 |
| **身份包格式演进**（早期数据结构不稳） | 存量资产迁移成本 | manifest 带版本号与迁移器；早期明确格式冻结时间点 |
| **oframe CLI 与 GUI 行为漂移** | 双端结果不一致 | 共享同一 Go 核心库，CLI 复用 GUI 管线；P2 用同一批用例双端回归 |

---

## 附：里程碑速览

- **P0 完成**：单方向全流程可用（定义→生成→预览→接受→导出）。
- **P1 完成**：4/8 方向自动镜像 + 质量评分 + 轻量编辑 + 版本化 + 可靠恢复。
- **P2 完成**：多引擎导出 + CLI + Windows 发布（公开 Beta）。
- **P3**：跨平台与扩展能力（视反馈决定）。

> 本计划与 `CONTEXT.md` 的领域语言（角色身份包、方向集、锚点、候选接受、身份版本、生成确认等）保持一致，后续设计文档沿用同一术语。
