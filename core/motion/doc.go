// Package motion defines motions, direction sets, and frame sequences — the
// structural model of animated behavior — together with the direction strategy
// (single direction by default, 4/8 directions with automatic mirroring).
//
// Domain language (CONTEXT.md): 动作 / 方向集 / 帧序列.
//
// This package is a skeleton in phase 1 (Go core & workspace); the data
// structures and operations land with the motion capability (tasks 3.1–3.6).
package motion
